{
  "name": "notes-functional",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "sst start",
    "dev": "cd ./frontend && yarn dev",
    "build": "sst build",
    "deploy": "sst deploy --stage=production",
    "remove": "sst remove",
    "console": "sst console",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@serverless-stack/cli": "^1.2.30",
    "@serverless-stack/resources": "^1.2.30",
    "@tsconfig/node16": "^1.0.3",
    "@types/uuid": "^8.3.4",
    "aws-cdk-lib": "2.24.0",
    "typescript": "^4.7.4",
    "vitest": "^0.16.0"
  },
  "workspaces": [
    "services"
  ]
}
